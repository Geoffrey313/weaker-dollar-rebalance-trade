# Dependencies for the reproduction pipeline.
# Provisional pins — to be re-frozen (pip freeze) once the pipeline is built (Phase 5).
pandas==2.2.2
pyarrow==17.0.0
numpy==1.26.4
scipy==1.13.1
statsmodels==0.14.2
matplotlib==3.9.2
pyyaml==6.0.2
# Data access (WRDS). Input-output tables (OECD ICIO) are parsed directly with pandas.
psycopg2-binary==2.9.9
wrds==3.2.0
